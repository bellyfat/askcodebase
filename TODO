1. 集成 xterm.js，支持传统的 Terminal 特性
2. 沉浸式，高亮当前 CommandBlock，按 ESC 或者 Command/Ctrl + Enter 可以离开当前 Terminal

BUGS:

3. ChatMessage 前景色没有适配 VSCode 主题；
4. 支持 Stop Conversation 和 Regenerate；

TEST CASES

1. creaate a new app.ts file in src folder and write a express server in it.
2. `Fix` Problems
3. `install` a colors npm package
4. `Run` shell commands
5. `refacor` extract to a function `utils.ts`

MileStone: 

1. 0 hand-crafted code
2. 一个完整的基于 Next.js todo list app 示例应用宣传视频，完全通过 AskCodebase AI 构建出来，100%通过聊天生成。

JS/TS + Python

Overview

1. 八月份（V2: Conversation, TypeChat, 内测|市场验证）
2. Team (Anaylst + Coder + Tester) => AutoGPT 软件架构师

版本排期

1周一个版本，每周日发布